const root = document.getElementById('root');
if (root) {
  root.innerHTML = '<div style="padding:16px;font-family:Arial, sans-serif">Admin UI build output missing. Run <code>npm --prefix admin run build</code> to generate the React bundle.</div>';
}
